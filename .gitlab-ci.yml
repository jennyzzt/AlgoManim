before_script:
  - python3 -m venv venv
  - source venv/bin/activate
  - pip install --upgrade pip
  - pip install -r requirements.txt

stages:
  - Lint
  - Test

pylint:
  stage: Lint
  allow_failure: true
  script:
    - pylint *

unit test:
  stage: Test
  script:
    - pytest -v --cov=algomanim